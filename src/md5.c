
#include "md5.h"

char    *ft_md5(char *m)
{
    WORD    *pm;
    size_t  m_len;

    // padding

    pm = (WORD *)m;
    // m_len = len(pm);
    while (/* condition */)
    {
        /* code */
    }
    
}